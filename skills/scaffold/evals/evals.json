{
  "skill_name": "scaffold",
  "evals": [
    {
      "id": "scaffold-nextjs-fullstack",
      "prompt": "/scaffold test/fixtures/nextjs-app",
      "expected_output": "Generated scaffold files for a Next.js fullstack project",
      "files": [],
      "expectations": [
        "Generates CLAUDE.md with Next.js-specific content (not generic template)",
        "Recommends superpowers plugin for TDD and debugging",
        "Recommends context7 plugin for Next.js and Prisma docs",
        "Does NOT generate a custom debug skill (superpowers covers it)",
        "Creates test-engineer agent for vitest + playwright",
        "Generates .mcp.json with postgres MCP server",
        "Generates .cursor/rules/project-context.mdc for Cursor",
        "Generates AGENTS.md for Codex",
        "Generates Prisma db-migrate custom skill (no official equivalent)",
        "Safety rules are generated for both Claude Code and Cursor"
      ],
      "assertions": [
        { "type": "file_exists", "path": "CLAUDE.md" },
        { "type": "file_exists", "path": "AGENTS.md" },
        { "type": "file_exists", "path": ".cursor/rules/project-context.mdc" },
        { "type": "file_contains", "path": "CLAUDE.md", "pattern": "[Nn]ext\\.?js|Next" },
        { "type": "file_not_contains", "path": "CLAUDE.md", "pattern": "\\[PROJECT_NAME\\]|PLACEHOLDER|TODO:" },
        { "type": "file_min_size", "path": "CLAUDE.md", "min_bytes": 200 },
        { "type": "file_min_size", "path": "AGENTS.md", "min_bytes": 200 },
        { "type": "dir_exists", "path": ".claude/rules" },
        { "type": "dir_exists", "path": ".cursor/rules" },
        { "type": "score_min", "dimension": "total_score", "min_value": 60 }
      ]
    },
    {
      "id": "scaffold-python-api",
      "prompt": "/scaffold test/fixtures/python-api",
      "expected_output": "Generated scaffold files for a Python FastAPI project",
      "files": [],
      "expectations": [
        "Detects Python and FastAPI correctly",
        "Generates CLAUDE.md mentioning FastAPI and pytest",
        "Recommends superpowers plugin",
        "Generates validate-api skill for OpenAPI spec",
        "Recommends ruff linting hook if ruff detected",
        "AGENTS.md contains Python-specific conventions"
      ],
      "assertions": [
        { "type": "file_exists", "path": "CLAUDE.md" },
        { "type": "file_exists", "path": "AGENTS.md" },
        { "type": "file_contains", "path": "CLAUDE.md", "pattern": "[Ff]ast[Aa][Pp][Ii]|FastAPI" },
        { "type": "file_contains", "path": "CLAUDE.md", "pattern": "pytest|py\\.test" },
        { "type": "file_not_contains", "path": "CLAUDE.md", "pattern": "\\[PROJECT_NAME\\]|PLACEHOLDER" },
        { "type": "file_min_size", "path": "CLAUDE.md", "min_bytes": 200 },
        { "type": "file_min_size", "path": "AGENTS.md", "min_bytes": 200 },
        { "type": "score_min", "dimension": "total_score", "min_value": 60 }
      ]
    },
    {
      "id": "scaffold-minimal-no-overgenerate",
      "prompt": "/scaffold test/fixtures/minimal",
      "expected_output": "Minimal scaffold for a simple project",
      "files": [],
      "expectations": [
        "Generates only CLAUDE.md and safety rules (not bloated)",
        "Does NOT create unnecessary agents",
        "Does NOT create skills for non-existent commands",
        "Recommends superpowers as the only plugin",
        "Total files generated is fewer than 8"
      ],
      "assertions": [
        { "type": "file_exists", "path": "CLAUDE.md" },
        { "type": "file_not_contains", "path": "CLAUDE.md", "pattern": "\\[PROJECT_NAME\\]|PLACEHOLDER|TODO:" },
        { "type": "file_min_size", "path": "CLAUDE.md", "min_bytes": 100 },
        { "type": "max_file_count", "base_dir": ".", "max_files": 8 },
        { "type": "score_min", "dimension": "total_score", "min_value": 40 }
      ]
    },
    {
      "id": "scaffold-official-first",
      "prompt": "/scaffold test/fixtures/nextjs-app",
      "expected_output": "Official plugins recommended before custom skills",
      "files": [],
      "expectations": [
        "Explicitly recommends superpowers plugin by name",
        "Explicitly recommends hookify plugin by name",
        "Does NOT generate a custom debugging skill",
        "Does NOT generate a custom TDD skill",
        "Does NOT generate a custom code-review skill",
        "Custom skills are ONLY for project-specific workflows"
      ],
      "assertions": [
        { "type": "file_exists", "path": "CLAUDE.md" },
        { "type": "no_skill_named", "pattern": "debug|tdd|code-review" },
        { "type": "score_min", "dimension": "specificity", "min_value": 15 }
      ]
    },
    {
      "id": "audit-stale-setup",
      "prompt": "/scaffold audit",
      "expected_output": "Audit report identifying stale or broken setup files",
      "files": [],
      "expectations": [
        "Scans .claude/ directory for existing files",
        "Checks skills reference actual commands from package.json",
        "Identifies any MCP servers for services not in project",
        "Reports findings categorized as Critical, Warning, or Info",
        "Offers to fix issues found"
      ],
      "assertions": [
        { "type": "output_contains", "pattern": "[Cc]ritical|[Ww]arning|[Ii]nfo" },
        { "type": "output_contains", "pattern": "[Aa]udit|[Rr]eport|[Ss]can" }
      ]
    },
    {
      "id": "codex-output-format",
      "prompt": "/scaffold test/fixtures/nextjs-app",
      "expected_output": "AGENTS.md generated in correct Codex format",
      "files": [],
      "expectations": [
        "Generates AGENTS.md at project root",
        "AGENTS.md contains Development Commands section with actual commands",
        "AGENTS.md contains Architecture section",
        "AGENTS.md contains Agent Instructions section",
        "AGENTS.md does NOT contain Claude Code-specific references"
      ],
      "assertions": [
        { "type": "file_exists", "path": "AGENTS.md" },
        { "type": "file_contains", "path": "AGENTS.md", "pattern": "##.*[Cc]ommand|##.*[Aa]rchitecture" },
        { "type": "file_not_contains", "path": "AGENTS.md", "pattern": "CLAUDE\\.md|\\.claude/" },
        { "type": "file_min_size", "path": "AGENTS.md", "min_bytes": 300 },
        { "type": "frontmatter_absent", "path": "AGENTS.md" }
      ]
    },
    {
      "id": "scaffold-monorepo",
      "prompt": "/scaffold https://github.com/vercel/turborepo",
      "expected_output": "Monorepo-aware scaffold with scoped agents",
      "files": [],
      "expectations": [
        "Detects monorepo structure (turbo.json or pnpm-workspace.yaml)",
        "Creates scoped agents per package boundary if distinct areas exist",
        "CLAUDE.md documents the monorepo structure and package responsibilities",
        "Rules include monorepo boundary guidance",
        "Does NOT treat the monorepo as a single flat project"
      ],
      "assertions": [
        { "type": "file_exists", "path": "CLAUDE.md" },
        { "type": "file_contains", "path": "CLAUDE.md", "pattern": "monorepo|workspace|packages" },
        { "type": "file_min_size", "path": "CLAUDE.md", "min_bytes": 400 },
        { "type": "score_min", "dimension": "total_score", "min_value": 60 }
      ]
    },
    {
      "id": "audit-duplicate-rules",
      "prompt": "/scaffold audit",
      "expected_output": "Audit detects duplicate rules across Claude Code and Cursor",
      "files": [],
      "expectations": [
        "Identifies rules with identical content in .claude/rules/ and .cursor/rules/",
        "Flags agents with overlapping responsibilities",
        "Reports duplicate count in the summary",
        "Suggests consolidation or removal for duplicates"
      ],
      "assertions": [
        { "type": "output_contains", "pattern": "[Dd]uplicate|[Oo]verlap|[Cc]onsolidat" }
      ]
    },
    {
      "id": "quality-gate-catches-hallucination",
      "prompt": "/scaffold test/fixtures/minimal",
      "expected_output": "Quality reviewer catches any hallucinated or incorrect content",
      "files": [],
      "expectations": [
        "Does NOT recommend MCP servers for services not detected in the project",
        "Generated CLAUDE.md does not mention frameworks the project does not use",
        "Skills reference only commands that actually exist in the project",
        "No placeholder or TODO content appears in generated files"
      ],
      "assertions": [
        { "type": "file_exists", "path": "CLAUDE.md" },
        { "type": "file_not_contains", "path": "CLAUDE.md", "pattern": "\\[PROJECT_NAME\\]|PLACEHOLDER|TODO:" },
        { "type": "no_placeholder_in_dir", "dir": ".claude" },
        { "type": "score_min", "dimension": "specificity", "min_value": 15 }
      ]
    },
    {
      "id": "discover-machine-scan",
      "prompt": "/scaffold discover test/fixtures",
      "expected_output": "DeveloperDNA generated with project discovery and cross-project analysis",
      "files": [],
      "expectations": [
        "Runs discover-orchestrator.sh and produces DeveloperDNA JSON",
        "Detects all test fixture projects (nextjs-app, python-api, minimal)",
        "Classifies developer role based on detected projects",
        "Identifies cross-project patterns (shared dependencies)",
        "Presents a preview summary before generating files",
        "Asks for user confirmation before proceeding"
      ],
      "assertions": [
        { "type": "output_contains", "pattern": "[Dd]iscover|[Dd]eveloper.*DNA|[Pp]rofile" },
        { "type": "output_contains", "pattern": "nextjs-app|python-api|minimal" },
        { "type": "output_contains", "pattern": "[Pp]roceed|[Gg]enerate|[Cc]onfirm" }
      ]
    },
    {
      "id": "discover-user-level-generation",
      "prompt": "/scaffold discover --user-level-only test/fixtures",
      "expected_output": "User-level AI setup generated at ~/.claude/",
      "files": [],
      "expectations": [
        "Generates user-level CLAUDE.md with developer profile",
        "Does NOT scaffold individual projects (--user-level-only flag)",
        "Classifies shared patterns as user-level vs project-level",
        "User-level rules are generic enough for all projects",
        "MCP servers in user-level .mcp.json use ${ENV_VAR} syntax, never hardcoded",
        "Quality review runs on user-level output before writing"
      ],
      "assertions": [
        { "type": "output_contains", "pattern": "user.level|~/.claude" },
        { "type": "output_contains", "pattern": "[Qq]uality|[Rr]eview|[Ss]core" }
      ]
    },
    {
      "id": "variant-dispatch-monorepo",
      "prompt": "/scaffold test/fixtures/nextjs-app",
      "expected_output": "Variant dispatch evaluated; monorepo variant NOT selected for non-monorepo project",
      "files": [],
      "expectations": [
        "Checks dispatch-table.json for variant signals",
        "Does NOT select monorepo variant (no turbo.json, nx.json, etc.)",
        "Does NOT select minimal variant (has framework detected)",
        "Proceeds with default SKILL.md for scaffold",
        "Generates standard scaffold output (not monorepo-scoped)"
      ],
      "assertions": [
        { "type": "file_exists", "path": "CLAUDE.md" },
        { "type": "file_not_contains", "path": "CLAUDE.md", "pattern": "monorepo|workspace|packages/" }
      ]
    },
    {
      "id": "variant-dispatch-minimal",
      "prompt": "/scaffold test/fixtures/minimal",
      "expected_output": "Minimal variant selected, generates only essential files",
      "files": [],
      "expectations": [
        "Checks dispatch-table.json and selects minimal variant",
        "Generates CLAUDE.md (required for all projects)",
        "Generates safety rule",
        "Does NOT generate custom agents for minimal project",
        "Does NOT generate custom skills for minimal project",
        "Total generated files is 2-4 (not bloated)"
      ],
      "assertions": [
        { "type": "file_exists", "path": "CLAUDE.md" },
        { "type": "file_min_size", "path": "CLAUDE.md", "min_bytes": 50 },
        { "type": "max_file_count", "base_dir": ".", "max_files": 5 }
      ]
    },
    {
      "id": "cross-project-deduplication",
      "prompt": "/scaffold discover test/fixtures",
      "expected_output": "Cross-project patterns identified and deduplicated between user and project levels",
      "files": [],
      "expectations": [
        "Identifies shared dependencies across test fixture projects",
        "Classifies patterns with frequency >= 50% as user-level",
        "Does NOT duplicate user-level patterns in project-level output",
        "Integration MCP servers are classified as user-level when detected",
        "Service relationships between projects are detected if applicable"
      ],
      "assertions": [
        { "type": "output_contains", "pattern": "user.level|shared|cross.project|dedup" }
      ]
    }
  ]
}
