[project]
name = "my-api"
version = "0.1.0"
dependencies = ["fastapi>=0.110.0", "uvicorn>=0.29.0", "redis>=5.0.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0.0", "ruff>=0.4.0"]

[tool.ruff]
line-length = 100
