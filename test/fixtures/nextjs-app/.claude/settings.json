{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=\"$FILE_PATH\"; case \"$file\" in */prisma/schema.prisma) echo 'Reminder: run npx prisma generate after schema changes' ;; esac; exit 0"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Read",
      "Glob",
      "Grep"
    ],
    "deny": []
  }
}
